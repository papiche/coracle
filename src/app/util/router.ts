/**
 * @deprecated Import from "src/app/util/navigation" or "src/app/util" instead.
 * This file is kept as a backward-compatibility barrel to avoid touching the 50+
 * existing imports across the codebase. The actual implementation has moved to
 * navigation.ts to prevent IDE auto-import confusion with src/util/router.ts
 * (the base routing engine).
 */
export * from "src/app/util/navigation"
